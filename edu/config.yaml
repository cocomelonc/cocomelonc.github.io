# local path to your repo (git clone https://github.com/cocomelonc/cocomelonc.github.io)
local_repo_path: "/path/to/your/cocomelonc.github.io" # <<< change this to your actual path

# vector db collection name
collection: "cocomelonc_blog" # changed collection name to differentiate

# ollama models
ollama_gen_model: "llama3.1:8b"       # example: "llama2", "mistral", "gemma"
ollama_embed_model: "nomic-embed-text" # example: "nomic-embed-text"

# which files to index (simple glob)
include_globs:
  - "_posts/**/*.md"
  - "*.md"

# optional excludes
exclude_globs:
  - ".git/**"
  - "_site/**"
  - "vendor/**"

# simple character chunking
chunk_chars: 1200
chunk_overlap: 200

# optional: used for pretty source links in answers
base_url: "https://github.com/cocomelonc/cocomelonc.github.io/blob/main/"